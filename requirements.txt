# ESG PDF Highlighter — Dependências
# Instala com: pip install -r requirements.txt

# Interface web
streamlit>=1.35.0

# Manipulação de PDF
pymupdf>=1.24.0        # fitz — highlight, busca de texto, anotações

# Utilitários padrão (já incluídos no Python, listados por clareza)
# json, argparse, re, sys, pathlib, tempfile — stdlib, sem instalação necessária
